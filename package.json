{
  "name": "kaneo-mcp",
  "version": "1.0.0",
  "description": "MCP server for the Kaneo project management API",
  "type": "module",
  "bin": {
    "kaneo-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "node --env-file=.env test/run-tests.mjs",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
